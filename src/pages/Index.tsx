import Startup from '@/components/Startup';

/**
 * Index route - redirects to appropriate home based on user role/device
 * This is the entry point that determines routing
 */
const Index = () => {
  return <Startup />;
};

export default Index;
